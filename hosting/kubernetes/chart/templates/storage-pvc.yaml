{{- $storageType := default "file" .Values.worknest.storage.type -}}
{{- $persistence := .Values.worknest.storage.file.persistence -}}
{{- if and (eq $storageType "file") $persistence.enabled (not $persistence.existingClaim) -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "worknest.storagePvcName" . }}
  labels:
    {{- include "worknest.labels" . | nindent 4 }}
spec:
  accessModes:
    {{- range $persistence.accessModes }}
    - {{ . }}
    {{- end }}
  resources:
    requests:
      storage: {{ required "worknest.storage.file.persistence.size must be set when creating a storage PVC" $persistence.size }}
{{- if $persistence.storageClass }}
  storageClassName: {{ $persistence.storageClass }}
{{- end }}
{{- end }}
