{{- if .Values.worknest.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "worknest.fullname" . }}
  labels:
    {{- include "worknest.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "worknest.fullname" . }}
  minReplicas: {{ .Values.worknest.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.worknest.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.worknest.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.worknest.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.worknest.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.worknest.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
